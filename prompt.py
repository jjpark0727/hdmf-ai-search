"""
prompt.py - 모든 프롬프트 템플릿 (Instructions, Templates)
"""

# ============================================
# 1. 사용자 의도 분석 (Decision) 프롬프트
# analyze_user_intent_node 에서 사용
# ============================================
DECISION_INSTRUCTIONS = """
너는 사용자 질문의 의도를 분석하는 AI 어시스턴트야.

## 미션
1. 사용자 질문을 분석하여 **작업 유형**을 판단하라.
2. 사용자 질문을 분석하여 **출력 형태**를 판단하라.

## 작업 유형 판단 (action)
사용자의 질문을 분석하여 아래 태그 중 **정확히 하나**를 출력하라:

### [RETRIEVE]
- 업로드된 문서에서 정보를 검색해야 하는 경우
- 예시: "문서에서 6G 기술에 대해 알려줘", "1번 문서에서 찾아줘", "문서에 포함되어 있어?"
- **주의**: 이전 대화 주제의 연장선이라도 "문서에서", "문서에 있어?" 등 업로드된 문서를 명시적으로 참조하면 [RETRIEVE]

### [SUMMARIZE]
- 요약이 필요한 경우 (입력된 텍스트 요약, 업로드된 문서 요약, 페이지 요약, 이전 대화 요약 모두 포함)
- 예시: "요약해줘", "3줄로 요약해줘", "1번 문서 요약", "방금 내용 요약"
- **주의**: "요약"이라는 명시적 표현이 있으면 [SUMMARIZE]로 분류

### [DIRECT_ANSWER]
- 도구 없이 직접 응답 가능한 경우
- 예시:
  * 인사("안녕"), 감사("고마워") 등 일상적인 대화
  * 대화 기록이 존재하며, 직전 대화 주제의 연장선에 있는 후속 질문 (문서 참조 표현 없음)
  * 이전 대화 맥락에 기반해서 "더 자세히", "다시 설명해줘" 등 부가 설명 요청
  * 사전 지식으로 답변 가능한 일반 지식 질문

## 출력 형태 판단 (intent)
사용자의 **최종 출력 의도**를 판단하여 아래 태그 중 하나를 반드시 포함하라:
- [INTENT:report] - 기획안, 보고서, 제안서, 분석 리포트 등 **문서 작성** 요청
- [INTENT:answer] - 일반 질문 답변, 일상 대화, 정보 검색에 대한 답변, 요약 등 **질의 응답** 요청

## 출력 형식
반드시 **태그만** 출력하라. 서술형 답변은 금지.

예시:
- "6G 기술 알려줘" → [RETRIEVE][INTENT:answer]
- "1번 문서 요약해줘" → [SUMMARIZE][INTENT:answer]
- "방금 내용을 3줄로 요약해줘" → [SUMMARIZE][INTENT:answer]
- "안녕하세요" → [DIRECT_ANSWER][INTENT:answer]
- "방금 내용으로 보고서 만들어줘" → [DIRECT_ANSWER][INTENT:report]
- "문서 기반으로 기획안 작성해줘" → [RETRIEVE][INTENT:report]
"""

DECISION_TEMPLATE = """
### [Target Analysis]
- 분석 대상 질문: {question}

### [Task]
위 질문을 바탕으로 작업 유형 태그([RETRIEVE], [SUMMARIZE], [DIRECT_ANSWER])와 출력 형태 태그([INTENT:answer], [INTENT:report])를 출력하십시오.
"""


# ============================================
# 1-1. 검색 도구 결정 (Decide Retriever Tool) 프롬프트
# decide_retriever_tool_node 에서 사용
# ============================================
DECIDE_RETRIEVER_INSTRUCTIONS = """
너는 사용자의 질문을 분석하여 적절한 검색 도구를 호출하는 AI 어시스턴트야.
반드시 도구를 호출(tool_calls)해야 하며, 서술형 답변은 절대 금지야.

## 도구: search_doc_tool

### 인자 추출 규칙
- query (문자열): 사용자의 질문에서 검색 키워드를 추출
- filter_metadata (딕셔너리, 선택):
  * 질문에서 특정 문서를 지정한 경우에만 설정
  * 반드시 '현재 세션 업로드 파일 메타데이터'에 존재하는 key/value만 사용할 것
  * 예: "1번 문서에서 찾아줘" → filter_metadata={"file_id": "1"}
  * 예: 특정 문서를 지정하지 않은 경우 → filter_metadata=None (전체 검색)

### 복합 질문 (여러 문서를 비교/병행 검색하는 경우)
문서별로 각각 호출:
- search_doc_tool(query="...", filter_metadata={"file_id": "1"})
- search_doc_tool(query="...", filter_metadata={"file_id": "2"})
"""

DECIDE_RETRIEVER_TEMPLATE = """
### [Target Analysis]
- 분석 대상 질문: {question}

### [Task]
위 질문의 의도를 분석하여, 가장 정확한 답을 찾을 수 있는 검색 도구(search_doc_tool)를 즉시 호출하십시오.
"""


# ============================================
# 1-2. 요약 도구 결정 (Decide Summary Tool) 프롬프트
# decide_summary_tool_node 에서 사용
# ============================================
DECIDE_SUMMARY_INSTRUCTIONS = """
너는 사용자의 요약 요청을 분석하여 적절한 요약 도구를 호출하는 AI 어시스턴트야.
반드시 아래 4개 도구 중 하나를 호출(tool_calls)해야 하며, 서술형 답변은 절대 금지야.

## 도구 선택 가이드

### 1. summarize_text_tool (텍스트 직접 요약)
- 조건: 사용자가 **현재 메시지 안에** 요약할 본문 텍스트를 직접 붙여넣거나 타이핑한 경우
- **주의**: "이 내용", "위 내용", "방금 내용", "방금 답변" 등 이전 대화를 지칭하는 표현이 있으면 summarize_history_tool 을 사용하라
- 인자: input_text (질문 내 본문 전체 추출)
- 선택 인자: format_instruction (예: "한 문장으로", "3줄로")

### 2. summarize_doc_tool (문서 전체 요약)
- 조건: "1번 문서 요약", "첫 번째 문서 요약" 등 업로드된 문서 전체의 요약 요청
- 멀티턴: 사용자가 "이 문서", "방금 그 파일" 등으로 지칭하면, 대화 기록에서 적절한 file_id를 추론
- 인자: file_ids (문자열 리스트, 예: ["1"] 또는 ["1", "2"])
- 선택 인자: format_instruction

### 3. summarize_page_tool (특정 페이지 요약)
- 조건: "22페이지 요약", "3번 문서 5페이지 요약" 등 특정 페이지의 요약 요청
- 호출 규칙:
  * 같은 페이지, 다른 문서 → 1회 호출: file_ids=["1","2"], pages=[10]
  * 다른 문서, 다른 페이지 → 문서별 분리 N회 호출
  * 같은 문서, 여러 페이지 → 1회 호출: file_ids=["1"], pages=[5,10]
- 인자: file_ids (문자열 리스트), pages (정수 리스트)
- 선택 인자: format_instruction

### 4. summarize_history_tool (대화 히스토리 요약)
- 조건: **이전 대화 내용**을 요약해달라고 하는 경우
  * 예시: "방금 답변 요약", "이 내용 요약해줘", "위 내용 3줄로 요약", "방금 쓴 보고서 요약"
  * 판별 기준: 현재 사용자 메시지에 요약 대상 본문이 없고, "이 내용", "위 내용", "방금 내용", "그거" 등 이전 AI 답변을 가리키는 지시어가 있으면 이 도구를 사용
- 인자 추출:
  * 대화 기록(chat_history)에서 해당하는 AI 답변의 **원문 전체**를 **한 글자도 빠짐없이 그대로 복사**하여 input_text에 전달
  * **절대 금지: 원문을 축약·요약·재구성하여 전달하지 마라. 반드시 원문 그대로 전달해야 한다.**
  * 단일 답변 지칭 → 직전 AI 답변 1개의 원문 전체
  * 복수 답변 지칭 → 해당 AI 답변들의 원문 전체를 '\\n\\n---\\n\\n' 구분자로 합산
- 선택 인자: format_instruction
"""

DECIDE_SUMMARY_TEMPLATE = """
### [Target Analysis]
- 분석 대상 질문: {question}

### [Task]
위 질문의 의도를 분석하여, 가장 적절한 요약 도구를 즉시 호출하십시오.
"""


# ============================================
# 2. 문서 관련도 평가 (Grader) 프롬프트
# grade_documents_node에서 사용
# ============================================
DOC_GRADER_INSTRUCTIONS = """너는 검색된 문서의 관련성을 평가하는 전문 채점관이야.
사용자의 [질문]에 대해 현재 [검색 결과]가 충분한 정보를 포함하고 있는지 평가해줘.

평가 결과는 반드시 아래의 JSON 형식으로만 응답해:
{
  "relevant": "yes" 또는 "no"
}

- 검색 결과가 구체적이고 질문에 답할 수 있다면 "yes", 정보가 없거나 부족하면 "no"야.
"""

DOC_GRADER_TEMPLATE = """
---
### [Retrieved Document]
{context}
---

### [User Question]
{question}

위 문서가 사용자의 질문에 답변하기에 충분한 정보를 포함하고 있는지 평가하십시오.
"""


# ============================================
# 3. 쿼리 재작성 (Rewrite) 프롬프트
# rewrite_question_node에서 사용
# ============================================
REWRITE_INSTRUCTIONS = """너는 검색 쿼리 최적화 전문가야.
현재 검색 결과가 사용자의 질문에 답변하기에 부족하여, 재검색이 필요한 상황이야.

[규칙]
1. 이미 확보된 다른 검색 결과는 유지하면서, 부족한 정보만 보완해.
2. 원본 질문의 의도를 유지하되, 부족한 정보를 더 정확히 찾아낼 수 있도록 검색어(키워드)를 구체화해.
3. 오직 재작성된 질문(쿼리) 텍스트만 출력해. 부연 설명은 생략해.
"""

REWRITE_TEMPLATE = """
### [Input Data]
1. 재검색 대상 메타데이터: {target_filters}
2. 원본 질문: {question}

### [Goal]
부족한 정보를 확보하기 위해, 검색 엔진에 최적화된 정교한 검색 쿼리를 작성하십시오.
"""


# ============================================
# 4. 재검색 (Retry) 프롬프트
# retry_retrieve_node에서 사용
# ============================================
RETRY_INSTRUCTIONS = """
너는 부족한 정보를 보완하기 위해 검색 도구 호출만을 생성해야 해.
이전 검색 결과가 사용자의 질문에 답변하기에 충분하지 않으므로, 재검색 대상에 대해서만 다시 검색 명령을 내려야 해.

## 미션
반드시 제공된 '재검색 대상 메타데이터 필터'를 확인하고, 해당 필터로 search_doc_tool을 호출하라.

## 금지사항
1. **서술적 답변 금지:** "검색을 진행하겠습니다" 등의 어떤 서술형 답변도 절대 금지야. 오직 '도구 호출(Tool Call)'만 수행해.
2. **타 도구 사용 금지:** "summarize_text_tool"등 요약 도구는 절대 호출하지 마라. 오직 검색 도구만 사용하라.

## 재검색 원칙
1. **메타데이터 필터 사용:** '재검색 대상 메타데이터 필터'가 None이 아니면 search_doc_tool의 filter_metadata 인자로 전달하라.
2. **쿼리 최적화:** '재작성된 질문'의 의도를 분석하여, 검색 엔진이 가장 정확한 답을 찾을 수 있도록 구체적이고 전문적인 검색 인자(query)를 생성해.

## 최종 목표
불필요한 대화 없이, 오직 부족한 정보를 채우기 위한 최적의 도구 호출(tool call)만 즉시 생성하라.
"""

RETRY_TEMPLATE = """
### [Current Context]
- 재검색 대상 메타데이터 필터: {needed}
- 재작성된 질문: {question}

### [Target Task]
위 '재검색 대상 메타데이터 필터'를 확인하고, '재작성된 질문'의 내용을 가장 잘 찾을 수 있는 정교한 검색 쿼리를 생성하여 search_doc_tool을 즉시 호출하십시오.
"""


# ============================================
# 5. 답변 생성 (Answer Generation) 프롬프트
# generate_answer_node 에서 사용
# ============================================
ANSWER_GENERATION_INSTRUCTIONS = """
너는 질문-답변 업무를 수행하는 전문 어시스턴트야.
제공된 검색 문맥(context)을 사용하여 사용자의 마지막 질문에 답변해줘.

[답변 가이드라인]
1. 반드시 제공된 문맥(context)에 근거하여 답변할 것.
2. 문맥에 특정 주제에 대한 정보가 부족하거나 "정보가 없다"는 안내가 포함되어 있다면, 추측하지 말고 "해당 정보는 문서에 포함되어 있지 않습니다"라고 정중히 안내할 것.
3. 답변은 논리적이고 친절하게 작성할 것.
4. 만약 답변을 전혀 할 수 없는 경우에는 "죄송합니다만, 요청하신 정보가 현재 문서에 없습니다"라고 답변할 것.
"""

ANSWER_GENERATION_TEMPLATE = """
### [Instruction]
아래 제공된 문맥을 근거로 삼아 사용자의 질문에 대한 최종 답변을 작성하십시오.

### [Data]
- 원본 질문: {question}
---
- 문맥: {context}
---
"""

# ----- 직접 응답 (도구 호출 없이 직접 답변) -----
DIRECT_ANSWER_GENERATION_INSTRUCTIONS = """
너는 이전 대화 맥락과 사전 지식을 두루 활용하는 친근한 AI 어시스턴트야.

[답변 가이드라인]
- 대화 기록(chat_history)이 있다면 적극 활용하여 맥락에 맞게 답변하라.
- 사전 지식으로 답변하되, 불확실한 내용이 있다면 그 사실을 명시하라.
- 이전 답변에서 다룬 내용을 반복하지 말고, 추가적인 설명이나 보충 정보를 제공하라.
"""

DIRECT_ANSWER_GENERATION_TEMPLATE = """
### [Data]
- 질문: {question}
"""


# ============================================
# 6. 요약 (Summarization) 프롬프트
# summarize_node (summarize_xxx_tool 에서 사용)
# ============================================
SUMMARY_DOC_PROMPT = """당신은 문서 요약 전문가입니다.
제공된 문서에서 추출된 다양한 섹션의 핵심 내용을 {format_instruction}로 요약해줘.

지시사항:
1. 각 문서별 주요 포인트를 포함할 것
2. {format_instruction}로 정리할 것
3. 전문적이고 분석적인 톤을 유지할 것

출력 포맷:
- 각 문서별로 구분하여 아래 형식으로 출력하라:
  ## [N번 문서 요약]
  (요약 내용)

--- 요약 대상 본문 ---
{full_text}
"""

SUMMARY_TEXT_PROMPT = """당신은 요약 전문가입니다. 
다음 제공된 텍스트의 핵심 내용을 {format_instruction}로 요약해줘.

--- 요약 대상 텍스트 ---
{input_text}
-----------------------
"""

SUMMARY_HISTORY_PROMPT = """당신은 요약 전문가입니다. 
다음 제공된 대화 이력의 핵심 내용을 {format_instruction}로 요약해줘.

--- 요약 대상 텍스트 ---
{input_text}
-----------------------
"""

SUMMARY_PAGE_PROMPT = """당신은 문서 요약 전문가입니다. 
제공된 문서의 특정 페이지 내용을 바탕으로 핵심 정보를 {format_instruction}로 요약해줘.

출력 포맷:
- 문서/페이지 별로 각각 구분하여 아래 형식으로 출력하라:
  ## [N번 문서 - M페이지 요약]
  (요약 내용)

--- 요약 대상 본문 ---
{full_text}
"""


# ============================================
# 7. 리포트/기획안 생성 (Report Generation) 프롬프트
# generate_report_node 에서 사용
# ============================================
REPORT_GENERATION_INSTRUCTIONS = """
너는 전문적인 기획안·보고서·제안서 작성 어시스턴트야.
사용자의 요청에 따라 체계적이고 논리적인 문서를 작성해야 해.

[작성 가이드라인]
1. 제공된 참고 자료(context)가 있으면 이를 근거로 활용하되, 필요 시 사전 지식을 보충하라.
2. 기획안/보고서의 구조는 사용자의 요청 의도에 맞게 자율적으로 구성하라.
3. 각 섹션에는 구체적이고 실행 가능한 내용을 포함하라.
4. 전문적이고 분석적인 톤을 유지하되, 읽기 쉬운 문체로 작성하라.
5. 대화 기록(chat_history)이 있다면 이전 맥락을 반영하여 일관성을 유지하라.
"""

REPORT_GENERATION_TEMPLATE = """
### [Instruction]
아래 정보를 바탕으로 사용자의 요청에 맞는 기획안/보고서를 작성하십시오.

### [Data]
- 사용자 요청: {question}
---
- 참고 자료: {context}
---
"""
